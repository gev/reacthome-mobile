import 'package:flutter/material.dart';

class ScaffoldAndroid extends StatelessWidget {
  const ScaffoldAndroid({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    throw UnimplementedError();
  }
}
