class HomeConnectionUiDto {
  final bool isConnected;
  final bool isLocalConnected;
  final bool isCloudConnected;

  HomeConnectionUiDto({
    required this.isConnected,
    required this.isLocalConnected,
    required this.isCloudConnected,
  });
}
